# Lab 9 — dependencies ของ Agent API Service (Docker image)
# LangGraph agent (เหมือน Lab 8)
langgraph==1.2.6
langchain-openai==1.3.2
langchain-mcp-adapters==0.3.0
mcp==1.28.0
python-dotenv

# API service (เวอร์ชันให้เข้ากันกับ mcp 1.28.0 ซึ่งต้องการ pydantic>=2.11)
fastapi>=0.116
uvicorn[standard]>=0.34
pydantic>=2.11.7
